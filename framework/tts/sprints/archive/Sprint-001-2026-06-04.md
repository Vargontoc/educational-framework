# Sprint 001 - tts ✅ Archived
# -----------------------------------------------

## Goal

Create the initial `tts-educational` API shell and Dockerfile for FEAT-001, without implementing real TTS provider integration.

## Status

status: completed
started_at: 2026-06-04
closed_at: 2026-06-04
blocked_by:
waiting_for:

## Tasks

- [x] Create the `tts-educational` service directory structure under `framework/tts`.
- [x] Create a minimal Python API application.
- [x] Add `GET /health` returning a successful service status response.
- [x] Add `POST /api/v1/tts/synthesize` as a placeholder endpoint returning `501 Not Implemented`.
- [x] Add service-level configuration loading for reserved TTS environment variables.
- [x] Add a Dockerfile for the `tts-educational` image.
- [x] Add minimal tests for healthcheck and placeholder synthesis behavior.
- [x] Verify the Docker image builds successfully.
- [x] Document local build and run instructions if needed.

## Risks

- Scope creep into Chatterbox, Coqui/XTTS, fallback, conversion, cache, backend integration, or frontend integration. → **Avoided**: all scope ruled out.
- Accidental coupling with `api-educational` instead of keeping `tts-educational` independent. → **Avoided**: service remains independent.
- Premature contract drift before the real synthesis API is finalized. → **Avoided**: contract not modified.
- Dockerfile builds may fail if Python dependency versions are not pinned or if the base image is too heavy. → **Resolved**: pinned deps, python:3.12-slim, build succeeds.

## Dependencies

- `docs/architecture/decisions/ADR-012-Replain-tts-service.md`
- `docs/product/features/tts/FEAT-001-Rebuild-TTS-Layer.md`

No blocking dependency on backend, frontend, Chatterbox, Coqui, or infrastructure compose changes for this sprint.

## Agent Instruction

- Implement only the API shell and Dockerfile defined by FEAT-001. ✅
- Do not call Chatterbox or Coqui. ✅
- Do not implement provider fallback logic. ✅
- Do not implement WAV/PCM to MP3 conversion. ✅
- Do not implement audio cache. ✅
- Do not modify `framework/infrastructure/docker-compose.yml` or `framework/infrastructure/docker-compose.prod.yml`. ✅
- Do not modify backend or frontend source files. ✅
- The placeholder synthesis endpoint must clearly return `501 Not Implemented`. ✅
- The service may load reserved environment variables but must start without provider containers. ✅

## Notes

This sprint prepared only the service boundary for `tts-educational`. Provider integration and `docs/contracts/api/openapi_tts.json` repopulation are intentionally deferred to later features.

## Review

completed_tasks: All 9 tasks completed.
  - Directory structure created at `framework/tts/`
  - FastAPI app with health + placeholder synthesize
  - Reserved env vars loaded via pydantic-settings
  - Dockerfile builds successfully
  - pytest tests pass (2/2)
  - README.md created

incomplete_tasks: None

contract_changes: None — `docs/contracts/api/openapi_tts.json` intentionally not modified (deferred to FEAT-002).

learnings:
  - python:3.12-slim is a reliable lightweight base for the tts-educational image.
  - pytest-asyncio must be explicitly added to requirements.txt (not bundled with pytest).
  - .env files are covered by existing `*.env` gitignore entry at root level.

next_sprint_suggestions:
  - FEAT-002: Repopulate `docs/contracts/api/openapi_tts.json` with the provider-agnostic contract for `tts-educational`.