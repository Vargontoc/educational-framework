# Sprint 002 - tts ✅ Archived
# -----------------------------------------------

## Goal

Repopulate `docs/contracts/api/openapi_tts.json` with the provider-agnostic OpenAPI contract for `tts-educational` as defined by FEAT-002.

## Status

status: completed
started_at: 2026-06-04
closed_at: 2026-06-04
blocked_by:
waiting_for:

## Tasks

- [x] Draft the `POST /api/v1/tts/synthesize` endpoint contract with provider-agnostic request schema.
- [x] Draft the `GET /api/v1/tts/status` endpoint contract.
- [x] Document `GET /health` contract.
- [x] Define error responses: `400`, `422`, `503`, `504`, `500`.
- [x] Document limits: max text length, supported locales, tones, output formats.
- [x] Ensure contract does not expose provider-native fields (`speaker_wav`, etc.).
- [x] Ensure contract does not include cache management endpoints.
- [x] Validate the final contract against FEAT-002 acceptance criteria.

## Risks

- Contract leaks provider-specific details (speaker_wav, etc.) into stable API. → **Avoided**: no speaker_wav in schema.
- Contract uses native Coqui/Chatterbox field names instead of semantic ones. → **Avoided**: all fields are semantic.
- Contract defines cache endpoints when cache ownership belongs to backend Avatar. → **Avoided**: no cache endpoints.
- Contract omits timeout behavior, making fallback unreliable for backend. → **Avoided**: timeoutMs=30000 documented.

## Dependencies

- `docs/architecture/decisions/ADR-012-Replain-tts-service.md`
- `docs/product/features/tts/FEAT-001-Rebuild-TTS-Layer.md`
- `docs/product/features/tts/FEAT-002-Contracts-API.md`

No blocking dependency on backend, frontend, infrastructure compose, or provider container changes.

## Agent Instruction

- Only update `docs/contracts/api/openapi_tts.json`. ✅
- Do not implement API routes or provider adapters. ✅
- Do not modify `framework/tts/app/` or `framework/tts/routes/`. ✅
- Do not modify docker-compose. ✅
- Do not modify backend or frontend source files. ✅
- Keep the contract provider-agnostic. ✅
- Do not document cache management endpoints. ✅
- Verify all acceptance criteria. ✅

## Review

completed_tasks:
  - Backed up old Coqui contract to openapi_tts.coqui.backup.json
  - Repopulated openapi_tts.json with provider-agnostic contract v1.0.0
  - Defined GET /health (liveness, no provider check)
  - Defined POST /api/v1/tts/synthesize (JSON body, audio/mpeg, 200/400/422/503/504/500)
  - Defined GET /api/v1/tts/status (configured state + loaded model, no live provider call)
  - Defined Error schema with stable code/message/retryable shape
  - Defined Limits schema (maxTextLength=300 configurable, locales=es/en, tones=5 abstract, outputFormats=mp3, timeoutMs=30000)
  - Documented response headers: X-Provider-Name, X-Model-Version, X-Voice-Profile-Version, X-Audio-Format-Version, X-Synthesis-Profile-Version
  - Added x-notes: provider_agnostic=true, cache_owner=backend-avatar, no_speaker_wav_in_contract=true
  - Validated JSON is well-formed

incomplete_tasks: None

contract_changes:
  - New contract (v1.0.0) replaces Coqui-native contract (v1.1.0)
  - Removed: GET /api/tts (Coqui native), /locales, /voices
  - Added: POST /api/v1/tts/synthesize, GET /api/v1/tts/status, GET /health
  - Request body: semantic fields (text, locale, tone, emotion, intensity, voiceProfile, outputFormat) — no speaker_wav
  - Response: audio/mpeg instead of audio/wav
  - Error schema: stable JSON with error.code for backend fallback mapping
  - Backed up old contract as openapi_tts.coqui.backup.json

learnings:
  - Contract describes tts-educational facade, not provider native APIs
  - tone is abstract enum to allow provider adapter mapping without coupling backend
  - status endpoint returns configured state only — avoids live provider coupling
  - Response headers provide cache-safe metadata without exposing provider internals

next_sprint_suggestions:
  - FEAT-003: Implement WAV/PCM to MP3 conversion module inside tts-educational