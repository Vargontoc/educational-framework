POSTGRES_USER=edu
POSTGRES_PASSWORD=change_me
POSTGRES_DB=edu_db
# Backup schedule example (not used by postgres itself): DAILY at 02:00 UTC
# Secrets must be provided via secret manager or CI; never commit real values.
