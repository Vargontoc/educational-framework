package es.vargontoc.educational.framework.game.model.memory;

public final class MemoryDefaults {

    private MemoryDefaults() {
    }

    /** Pair attempts per pair up to which the game earns 3 stars (1.5 = one extra try every two pairs). */
    public static final double THREE_STARS_MAX_ATTEMPTS_PER_PAIR = 1.5;
    /** Pair attempts per pair up to which the game earns 2 stars. */
    public static final double TWO_STARS_MAX_ATTEMPTS_PER_PAIR = 2.5;
}
