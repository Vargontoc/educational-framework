type: object
additionalProperties: false

properties:
  id:
    type: integer
    format: int64
  childProfileId:
    type: integer
    format: int64
  achievementCode:
    type: string
  activityId:
    type: integer
    format: int64
  topicId:
    type: integer
    format: int64
  earnedAt:
    type: string
    format: date-time
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
