summary: Get diary activities by period
operationId: getDiaryActivities
tags:
  - diary-controller
parameters:
  - name: childProfileId
    in: path
    required: true
    schema:
      type: integer
      format: int64
  - name: period
    in: query
    required: false
    schema:
      type: string
      enum: [TODAY, WEEK, MONTH, ALL]
      default: WEEK
responses:
  "200":
    description: OK
    content:
      "*/*":
        schema:
          $ref: "../../schemas/diary/api-diary-activities-response.yaml"
