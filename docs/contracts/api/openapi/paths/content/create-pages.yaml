summary: Create page
operationId: createPage
tags:
  - story-controller
parameters:
  - name: storyId
    in: path
    required: true
    schema:
      type: integer
      format: int64
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: "../../schemas/content/create-story-page-request.yaml"
responses:
  "200":
    description: OK
    content:
      "*/*":
        schema:
          $ref: "../../schemas/content/api-story-page-response.yaml"
